import {Region} from "./region";

export class Customer {
  id: number;
  numberID: string;
  firstName: string;
  lastName: string;
  email: string;
  photoUrl: string;
  region: Region;
  state: string;
}
